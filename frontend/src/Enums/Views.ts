export enum Views {
    // Enum for different views in the application
    // Used to manage the current view in the Redux store
    WELCOME = 'WELCOME',
    POINTS = 'POINTS',
    PACKAGES = 'PACKAGES',
}